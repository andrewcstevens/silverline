{
  "name": "silverline-reeding",
  "version": "0.1.0",
  "private": true,
  "description": "Reeding evidence layer — immutable Assay record, append-only Census, Grade computation (Lane B).",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.10",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.2"
  }
}
